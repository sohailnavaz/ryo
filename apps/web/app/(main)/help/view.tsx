'use client';
import { GuestIncidentsScreen } from '@bnb/features';

// Support hub — raise an issue and track it through to resolution. The guest
// incident lands in the admin queue (support→incident loop).
export default function View() {
  return <GuestIncidentsScreen />;
}
