'use client';
import { HomeScreen } from '@bnb/features';

// Home is search-first (clean). The GlobeExplorer component is kept in this
// folder, parked — ready to move to its own /globe tab or refine before reuse.
export default function Page() {
  return <HomeScreen />;
}
